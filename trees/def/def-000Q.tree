\title{Linearly dependent}
\taxon{Definition}
\p{
    A set of vectors #{\{v_1, \dots, v_n\}} is called \strong{linearly dependent} if
    ##{a_1 v_1 + \dots + a_n v_n = 0}
    for some #{a_1, \dots, a_n \in \mathbb{F}} with at least one #{a_i \neq 0} (not all #{0}).
}