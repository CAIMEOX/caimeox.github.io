\title{Map to bigger dimension is not surjective}
\taxon{Lemma}
\p{
    Let #{V} and #{W} be finite-dimensional vector spaces, 
    and #{\dim V < \dim W}.
    Then no linear map #{T:V\to W} is surjective.
}