\title{Divergent Combinator}
\taxon{Example}
\import{macros}
\p{
    An interesting example named \strong{omega combinator} of beta reduction is the following:
    ##{
        \appl{(\lambda x.\appl{x}{x})}{(\lambda x.\appl{x}{x})}
        \tob
        \appl{(\lambda x.\appl{x}{x})}{(\lambda x.\appl{x}{x})}
    }
    The result of the beta reduction is the same term as the original term,
    and never terminates.
}